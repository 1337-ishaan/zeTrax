import styled from 'styled-components/macro';

const CreateBtcWrapper = styled.div``;

interface CreateBtcProps {}

const CreateBtc = ({}: CreateBtcProps): JSX.Element => {
  return <CreateBtcWrapper>Create BTC</CreateBtcWrapper>;
};

export default CreateBtc;
