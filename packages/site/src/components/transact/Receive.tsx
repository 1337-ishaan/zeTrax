import styled from 'styled-components/macro';

const ReceiveWrapper = styled.div``;

interface ReceiveProps {}

const Receive = ({}: ReceiveProps): JSX.Element => {
  return <ReceiveWrapper>receive</ReceiveWrapper>;
};

export default Receive;
